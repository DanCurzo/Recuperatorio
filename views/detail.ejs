<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <title>Detalle de películas</title>
</head>

<body>
    <div class="m-2">
        <% if(movie) { %>
            <div style="display: flex; flex-direction: row; align-items: center; justify-content: space-between;">
                <h1>
                    Detalles de la película: <%= movie.title %>
                </h1>
                <a href="/">
                    <button class="btn btn-outline-primary">
                        🏠
                    </button>
                </a>
            </div>
            <hr>
            <% if(movie.rating) { %>
                <p>Rating:: <%= movie.rating %></p>
            <% } else { %>
                <p>Rating: Sin especificar.</p>
            <% } %>

            <% if(movie.awards) { %>
                <p>Premios: <%= movie.awards %></p>
            <% } else { %>
                <p>Premios: Sin especificar.</p>
            <% } %>

            <% if(movie.length) { %>
                <p>Duraación: <%= movie.length %></p>
            <% } else { %>
                <p>Duración: Sin especificar.</p>
            <% } %>

            <% if(movie.genre) { %>
                <p>Género: <%= movie.genre.name %></p>
            <% } else { %>
                <p>Género: Sin especificar.</p>
            <% } %>

            <% if(movie.release_date) { %>
                <p>Lanzamiento: <%= movie.release_date %></p>
            <% } else { %>
                <p>Lanzamiento: Sin especificar.</p>
            <% } %>

            <h3>Actores:</h3>
            <% movie.actors.forEach(actor=> { %>
                <p>
                    <%= actor.first_name %> <%= actor.last_name %>
                </p>
                <% }) %>

        <% } else { %>
            <h2>No pudimos encontrar el detalle de la película.</h2>
        <% } %>
    </div>
</body>

</html>